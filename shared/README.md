# shared/

Bất biến dùng chung (AD-12, AD-23): `config.py` (env), `ids.py` (scene_id bất biến + timecode ms), `storage.py` (storage-port + adapter filesystem), `db.py` (Postgres SoT), `models.py` (schema Video/Scene cơ sở).
