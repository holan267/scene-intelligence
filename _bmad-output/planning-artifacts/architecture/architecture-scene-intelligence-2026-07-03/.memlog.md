---
scope: Scene Intelligence — hệ tìm kiếm cảnh video on-prem cho đài truyền hình
purpose: Kiến trúc spine cho MVP (ingest pipeline + hybrid search), on-prem, tiếng Việt, kho hàng chục nghìn giờ
altitude: initiative -> features (toàn hệ MVP)
updated: 2026-07-03T14:40
---

- (decision) Working mode = Fast path; deliverable = chỉ spine (build substrate). Kế thừa im lặng các quyết định kỹ thuật trong addendum PRD
- (constraint) Kế thừa từ PRD/addendum (read-only): scene=đơn vị tìm; 1 scene_embedding gộp; phễu search 4 tầng; lưu 3 kho (Postgres+VectorDB+ObjectStorage); pgvector->Qdrant; tiếng Việt first-class; on-prem thuần; MVP defer NLE round-trip & analytics
- (version) Web-verified mid-2026 (stack-verification.md): PySceneDetect 0.7; PhoWhisper-large qua faster-whisper (Vi ASR); InsightFace 1.0 buffalo_l; YOLO26; SigLIP2; EasyOCR+VietOCR(pbcquoc Python); Qwen3-VL; bge-reranker-v2-m3; BGE-M3 (text embed); FastAPI 0.139; PG18+pgvector 0.8.4; Qdrant 1.17.1; React 19.2+Vite 7; vLLM serving
- (decision) ĐỔI object storage: MinIO CE khai tử 2/2026 -> dùng S3-compatible on-prem khác (SeaweedFS/Garage/Ceph RGW). Prevents: build lên hạ tầng đã chết
- (decision) Nâng Vi ASR: PhoWhisper-large (không Whisper base) để đạt 'tiếng Việt first-class'; scene_embedding dùng BGE-M3; rerank bge-reranker-v2-m3; VLM = Qwen3-VL
- (decision) PARADIGM: pipes-and-filters ingest (ghi) tách khỏi search service (đọc) = CQRS-lite, quanh xương sống 3 kho. Prevents: trộn nhịp ghi-nặng-GPU với đọc-nhẹ-nhanh
- (decision) OWNERSHIP: single-writer/domain. Pipeline sở hữu enrichment (Scene metadata+embedding); API sở hữu user-state (mark-used, auth). Không component nào ghi 2 domain. Prevents: hai chủ một entity
- (decision) SoT: Postgres = nguồn sự thật Scene metadata + scene_document + user-state. Vector store & FTS index = DẪN XUẤT, rebuild được từ Postgres. Prevents: drift Postgres<->vectorDB
- (decision) INGEST idempotent+resumable, key (video_id,scene_id,stage); mỗi stage chỉ ghi field-namespace của mình. Prevents: nhân đôi + đè kết quả stage khác
- (decision) DEPLOY: container hoá, Docker Compose 1 node GPU cho MVP, air-gap được; k8s deferred. STACK: Python (pipeline+FastAPI) + React19/Vite UI; UI chỉ qua REST/JSON, không đụng DB. TIMECODE canonical = *_ms integer, SMPTE ở UI
- (decision) EVAL harness = component hạng nhất, chạy qua search service THẬT (không nhánh code riêng). Vietnamese-first: cấm model English-only trong đường NL
- (event) Draft spine xong (16 AD, 3 diagram, stack verified). Lint pass (2 false-positive đã sửa). Chờ user review / quyết reviewer gate
- (event) Reviewer Gate: 3 reviewer xong (reviews/). Adversarial=CONDITIONAL ~10 seam; Rubric=yếu operational envelope+API contract; Version=stack OK, yếu LICENSE (YOLO26 AGPL, InsightFace non-commercial)
- (decision) FIX identity: scene_id = id ỔN ĐỊNH bất biến gán lúc detect đầu (UUID/content-hash), KHÔNG positional; re-detect ánh xạ vào scene cũ, không re-mint. Prevents: user-state 'used' dán nhầm nội dung khác sau re-ingest (AD-1 tighten)
- (decision) FIX freshness+visibility: derived (embedding/FTS) mang doc_version(checksum scene_document); scene chỉ vào kết quả khi search_status=indexed set nguyên tử sau khi ghi xong mọi derived-store. Prevents: rank bản cũ / scene nửa-index lọt kết quả (AD-17, AD-18 mới)
- (decision) FIX ownership: (a) job/queue = domain riêng do orchestrator sở hữu, worker chỉ chuyển state task mình (AD-19); (b) enrich ghi CỘT riêng, không read-modify-write JSONB chung (AD-5 tighten); (c) clip/thumbnail chỉ qua API cùng auth, UI không chạm object storage trực tiếp (AD-20)
- (decision) FIX AD-7: MVP đúng 1 scene_embedding/scene; sub-scene chunking DEFER v2; RRF gộp ở mức scene (1 candidate/scene trước rerank). FIX eval: eval chạy search ở chế độ tất định (rerank bật, ngưỡng cố định, bỏ cache) -> recall@10/MRR tái lập được (AD-21)
- (decision) FIX filter schema: tập thuộc tính lọc (FR-7) = 1 schema dùng chung ở shared/, ingest ghi & search/UI lọc cùng bind (AD-22). FIX backup: Postgres SoT + media gốc = backup-critical (không rebuild được); derived rebuild được (AD-23). Query-understanding: MVP KHÔNG LLM parse, coi query là semantic + filter UI tường minh; NL->filter defer v2
- (decision) FIX stack/license: object storage default = SeaweedFS (Apache-2.0); pgvector thêm halfvec+pgvectorscale làm bậc trước Qdrant; vLLM>=0.11.0; PhoWhisper cần convert CTranslate2; cân nhắc ViRanker (rerank tiếng Việt). YOLO26 (AGPL) + InsightFace buffalo_l (non-commercial) = RỦI RO LICENSE -> rà/thay trước thương mại hoá (RT-DETR Apache thay YOLO)
- (event) spine finalized — 22 AD, lint clean, Reviewer Gate applied (adversarial seams vá, operational envelope bổ sung, license flagged). status=final
- (decision) UPDATE (Winston): bỏ object storage S3-compat/SeaweedFS -> media storage = filesystem nội bộ (NAS/SAN cho kho thật, ổ local cho dev), mounted volume. AD-11/19/22 cập nhật (media qua NAS/local; API stream/proxy thay signed-URL). Thêm AD-23 storage-port trừu tượng (put/get/stream theo media-key) để đổi sang S3 sau chỉ là config+adapter. Lưu ý: kho chục nghìn giờ cần NAS/SAN, ổ local chỉ hợp dev
- (event) spine updated (storage decision) — 23 AD
